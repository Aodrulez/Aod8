
mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,39
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,39
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,72
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,114
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,48
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,39
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,39
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,87
add a,4
mov [sp],a
output

mov a,39
add a,4
mov [sp],a
output

mov a,89
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,68
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,54
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,76
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,95
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,42
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,54
add a,4
mov [sp],a
output

mov a,37
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output




mov a,99
mov sp,a
mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,41
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,41
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,43
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,41
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,88
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,40
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,59
add a,3
mov [sp],a
inc sp

mov a,42
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,57
add a,3
mov [sp],a
inc sp

mov a,90
add a,3
mov [sp],a
inc sp

mov a,123
add a,3
mov [sp],a
inc sp

mov a,7
add a,3
mov [sp],a
inc sp




mov a,0
mov sp,a
mov [sp],a
inc a
inc a
inc a
inc sp
mov [sp],a







mov a,99
mov sp,a
interprt:
mov b,[sp]
cmp b,62
je pointerplus
cmp b,60
je pointerminus
cmp b,46
je printf
cmp b,44
je scanf
cmp b,43
je incx
cmp b,45
je decx
cmp b,91
je lupstart
cmp b,93
je fixme
cmp b,126
je yay1
ret:
inc a
mov sp,a
loop interprt



pointerplus:
mov b,1
mov sp,b
mov b,[sp]
add b,1
mov [sp],b
mov b,0
loop ret


pointerminus:
mov b,1
mov sp,b
mov b,[sp]
sub b,1
mov [sp],b
mov b,0
loop ret

nop
fixme:
jmp fux

printf:
mov b,1
mov sp,b
mov b,[sp]
mov sp,b
output
mov b,0
loop ret


nop
boo:
loop ret

scanf:
mov b,1
mov sp,b
mov b,[sp]
mov sp,b
input
mov b,0
loop ret

jmp 4
yay1:
jmp yay2



incx:
mov b,1
mov sp,b
mov b,[sp]
mov sp,b
mov b,[sp]
inc b
mov [sp],b
mov b,0
loop ret

junk:
loop boo

decx:
mov b,1
mov sp,b
mov b,[sp]
mov sp,b
mov b,[sp]
sub b,1
mov [sp],b
mov b,0
loop ret



fux:
jmp lupend




goback:
loop junk

lupstart:
mov b,1
mov sp,b
mov b,[sp]
mov sp,b
mov b,[sp]
cmp b,0
jne pissonu
wtf:
inc a
mov sp,a
mov b,[sp]
cmp b,91
je jerk
cmp b,93
je save
loop wtf
jerk:
mov b,0
mov sp,b
mov b,[sp]
inc b
mov [sp],b
loop wtf


yay2:
jmp yay3

pissonu:
loop goback
save:
mov b,0
mov sp,b
mov b,[sp]
cmp b,0
jne roar
stick:
loop boo

roar:
mov b,0
mov sp,b
mov b,[sp]
sub b,1
mov [sp],b
loop wtf


plum:
loop stick


lupend:
ftw:
sub a,1
mov sp,a
mov b,[sp]
cmp b,93
je jarhead
cmp b,91
je manna
loop ftw
manna:
mov b,0
mov sp,b
mov b,[sp]
cmp b,0
jne lambda
sub a,1
loop plum
lambda:
mov b,0
mov sp,b
mov b,[sp]
sub b,1
mov [sp],b
loop ftw
jarhead:
mov b,0
mov sp,b
mov b,[sp]
inc b
mov [sp],b
loop ftw


yay3:

mov a,6
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,39
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,39
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,72
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,72
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,80
add a,4
mov [sp],a
output

mov a,100
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,100
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,105
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,98
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,72
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,114
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,48
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,115
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,35
add a,4
mov [sp],a
output

mov a,82
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,113
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,73
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,95
add a,4
mov [sp],a
output

mov a,100
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,35
add a,4
mov [sp],a
output

mov a,42
add a,4
mov [sp],a
output

mov a,80
add a,4
mov [sp],a
output

mov a,100
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,96
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,117
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,40
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,95
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,105
add a,4
mov [sp],a
output

mov a,105
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,105
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,99
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,105
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,115
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,40
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,65
add a,4
mov [sp],a
output

mov a,116
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,108
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,95
add a,4
mov [sp],a
output

mov a,103
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,43
add a,4
mov [sp],a
output

mov a,108
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,95
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,117
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,114
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,113
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,105
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,95
add a,4
mov [sp],a
output

mov a,100
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,96
add a,4
mov [sp],a
output

mov a,96
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,100
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,117
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,98
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,95
add a,4
mov [sp],a
output

mov a,113
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,117
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,94
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,95
add a,4
mov [sp],a
output



mov a,93
add a,4
mov [sp],a
output

mov a,113
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,115
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,100
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,82
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,113
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,73
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,95
add a,4
mov [sp],a
output

mov a,100
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,94
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,99
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,105
add a,4
mov [sp],a
output

mov a,108
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,105
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,96
add a,4
mov [sp],a
output

mov a,40
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,94
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,95
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,105
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,114
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,117
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,96
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,113
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,114
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,100
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,95
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,113
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,99
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,100
add a,4
mov [sp],a
output

mov a,105
add a,4
mov [sp],a
output

mov a,42
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,62
add a,4
mov [sp],a
output

mov a,117
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,95
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,105
add a,4
mov [sp],a
output

mov a,108
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,99
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,100
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,95
add a,4
mov [sp],a
output

mov a,100
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,99
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,40
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,117
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,113
add a,4
mov [sp],a
output

mov a,35
add a,4
mov [sp],a
output

mov a,114
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,113
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,96
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,96
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,100
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,94
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,95
add a,4
mov [sp],a
output

mov a,40
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,105
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,96
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,34
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,105
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,98
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,100
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,96
add a,4
mov [sp],a
output

mov a,114
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,95
add a,4
mov [sp],a
output


mov a,97
add a,4
mov [sp],a
output

mov a,96
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,95
add a,4
mov [sp],a
output

mov a,100
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,109
add a,4
mov [sp],a
output

mov a,113
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,34
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,103
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,98
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,78
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,114
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,65
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,99
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,99
add a,4
mov [sp],a
output

mov a,42
add a,4
mov [sp],a
output

mov a,120
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,39
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,39
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

sleep 1
sleep 1
mov a,6
add a,4
mov [sp],a
output

mov a,87
add a,4
mov [sp],a
output

mov a,39
add a,4
mov [sp],a
output

mov a,89
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,63
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,99
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,113
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,113
add a,4
mov [sp],a
output

mov a,95
add a,4
mov [sp],a
output

mov a,95
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,98
add a,4
mov [sp],a
output

mov a,113
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,117
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,95
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,105
add a,4
mov [sp],a
output

mov a,108
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,99
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,100
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,73
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,63
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,46
add a,4
mov [sp],a
output

mov a,44
add a,4
mov [sp],a
output

mov a,45
add a,4
mov [sp],a
output

mov a,45
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,35
add a,4
mov [sp],a
output

mov a,63
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,108
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,113
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,100
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,73
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,63
add a,4
mov [sp],a
output

mov a,100
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,99
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,35
add a,4
mov [sp],a
output

mov a,29
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,87
add a,4
mov [sp],a
output

mov a,29
add a,4
mov [sp],a
output

mov a,89
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,73
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,96
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,96
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,108
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,100
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,116
add a,4
mov [sp],a
output

mov a,108
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,100
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,115
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,117
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,113
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,114
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,96
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,100
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,63
add a,4
mov [sp],a
output

mov a,80
add a,4
mov [sp],a
output

mov a,73
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,95
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,105
add a,4
mov [sp],a
output

mov a,60
add a,4
mov [sp],a
output

mov a,105
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,95
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,42
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,99
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,42
add a,4
mov [sp],a
output

mov a,61
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,95
add a,4
mov [sp],a
output

mov a,100
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,100
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,40
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,95
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,108
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,40
add a,4
mov [sp],a
output

mov a,71
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,117
add a,4
mov [sp],a
output

mov a,99
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,98
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,72
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,114
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,47
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,34
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,72
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,114
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,48
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,100
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,117
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,113
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,115
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,95
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,105
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,117
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,113
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,108
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,118
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,42
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,54
add a,4
mov [sp],a
output

mov a,37
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,74
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,54
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,80
add a,4
mov [sp],a
output

mov a,100
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,113
add a,4
mov [sp],a
output

mov a,94
add a,4
mov [sp],a
output

mov a,105
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,100
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,113
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,96
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,95
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,114
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,96
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,81
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,105
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,54
add a,4
mov [sp],a
output

mov a,76
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,115
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,96
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,95
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,105
add a,4
mov [sp],a
output

mov a,94
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,98
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,117
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,113
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,74
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,95
add a,4
mov [sp],a
output

mov a,103
add a,4
mov [sp],a
output

mov a,43
add a,4
mov [sp],a
output

mov a,74
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,105
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,98
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,94
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,100
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,72
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,114
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,47
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,115
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,111
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,72
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,114
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,48
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,112
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,94
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,109
add a,4
mov [sp],a
output

mov a,113
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,98
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,96
add a,4
mov [sp],a
output

mov a,42
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,68
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,114
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,83
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,96
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,98
add a,4
mov [sp],a
output

mov a,113
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,96
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,117
add a,4
mov [sp],a
output

mov a,29
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,63
add a,4
mov [sp],a
output

mov a,80
add a,4
mov [sp],a
output

mov a,73
add a,4
mov [sp],a
output

mov a,41
add a,4
mov [sp],a
output

mov a,46
add a,4
mov [sp],a
output

mov a,44
add a,4
mov [sp],a
output

mov a,45
add a,4
mov [sp],a
output

mov a,45
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,54
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,62
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,95
add a,4
mov [sp],a
output

mov a,100
add a,4
mov [sp],a
output

mov a,101
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,96
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,98
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,61
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,96
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,113
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,97
add a,4
mov [sp],a
output

mov a,118
add a,4
mov [sp],a
output

mov a,42
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output

mov a,36
add a,4
mov [sp],a
output

mov a,95
add a,4
mov [sp],a
output

mov a,37
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,73
add a,4
mov [sp],a
output

mov a,93
add a,4
mov [sp],a
output

mov a,104
add a,4
mov [sp],a
output

mov a,95
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,106
add a,4
mov [sp],a
output

mov a,42
add a,4
mov [sp],a
output

mov a,107
add a,4
mov [sp],a
output

mov a,110
add a,4
mov [sp],a
output

mov a,99
add a,4
mov [sp],a
output

mov a,28
add a,4
mov [sp],a
output

mov a,6
add a,4
mov [sp],a
output
halt



